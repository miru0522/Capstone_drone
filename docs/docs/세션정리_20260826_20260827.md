# 세션정리 - 2026-08-26 ~ 2026-08-27

VSCode Claude Code 환경 설정 + 드론 코드 안전점검/수정 + 서버-드론 텔레메트리 정합성 점검 + 전체 백업정책을 git 단일 저장소(Capstone_drone)로 통합

## 1. VSCode / SSH 환경 설정

- VSCode에 Remote-SSH 확장 설치
- `~/.ssh/config`에 `HPC-server`(기존), `Jetson`(신규, hpc@ubuntu, Tailscale) alias 등록
- Jetson용 SSH 키: 기존 `id_ed25519`(HPC-server와 공유) 재사용. 팀원 비밀번호 로그인 방식은 그대로 유지(sshd_config 안 건드림)
- 참고: 기존 `id_ed25519`는 passphrase 없음 - PC가 털리면 두 시스템 모두 접근 가능한 리스크 있음(조치 보류, 인지만 해둠)

## 2. 서버 사용 가이드라인 문서 확인

- `서버_사용_가이드라인_요약.docx` 확인: yunseon 계정 공유(로그로 작업자 구분 불가), `/home/yunseon` 밖 절대 건드리지 않기, `rm -rf` 금지 목록, GPU/Docker 확인 후 사용, 대용량 작업 전 `df -h` 확인(90% 이상이면 중단)

## 3. 드론 코드(Jetson) 전체 리뷰 - 이슈 10건 발견, 9건 수정

`main.py`, `ring_buffer.py`, `uploader.py`, `anomaly_model.py`, `command_receiver.py`, `waypoint_mission.py`, `telemetry_sender.py`, `audio_receiver.py`, `start_all.sh` 전체 리뷰.

**수정 완료:**
1. 배터리 % 스케일 판정 휴리스틱(`if pct<=1.0: pct*=100`) 제거 → MAVSDK 스펙대로 항상 고정 x100. 기존 코드는 저배터리(1% 미만) 위급상황을 오판할 수 있는 구간이 있었음(2026-07-18 세션 기록 확인 - 원래 이중곱셈 버그 방지용 땜질이었음)
2. LAND 완료 감시 태스크(`_wait_landed`)가 `self._watch_task`로 추적 안 되던 버그 수정 - 다른 명령 개입 시 취소 안 되고 나중에 상태를 강제로 덮어쓸 수 있었음
4. `ring_buffer.py` docstring 오타 수정(추론창 "5초/45프레임" → 실제 "1초/9프레임")
6. `CANCEL_TIMEOUT_SEC` → `EMERGENCY_STOP_TIMEOUT_SEC` 리네임(구스펙 잔재 정리)
7-8. 레거시 파일(`audio_receiver.py`, `fastapi_ws_server.py`, `jetson_ws_client*.py`, `test_web_client*.py`) → `legacy/` 이동 + 정체불명 0바이트 파일 5개 삭제
9. `WideBranchNetTRT` 출력 shape에 `TRTDetector`와 동일한 `max(d,1)` 방어 클램프 추가
10. `detect_anomaly()` 연속 추론실패 10회 이상 시 critical 로그로 경고 강화

**보류(검증/협의 필요):**
3. `CANCEL_PATROL`이 상태(LANDING 포함) 무관하게 항상 수락 - 서버도 게이팅 없이 그냥 전달만 함. LANDING 중 `hold()` 실제 PX4 동작은 실기 검증 필요.
5. `SET_ROUTE`/`START_PATROL` 경합조건 - 근본원인 확인(§5), 수정은 서버 코드라 보류.

모든 수정은 "최신상태 확인 → 백업 → 편집 → `ast.parse` 문법검사 → 업로드 → 재검사 → 재시작 → 프로세스 생존 확인 → git commit+push" 순서로 진행.

★ 사용자 확인: "Claude Code 세션 중 Jetson은 항상 개발모드(배터리 미장착)" - 이후 재시작 전 매번 안전 확인 안 함.

## 4. 코드 백업 정책 1차 전환: Google Drive → git (`drone_2026` 단독 저장소)

- GitHub 비공개 저장소 `miru0522/drone_2026` 생성, Jetson 전용 배포키로 `~/drone_2026/code/`에서 직접 커밋/push하는 구조로 시작
- (이후 §7에서 `Capstone_drone`으로 통합되며 이 저장소는 폐기됨)

## 5. 서버 코드(HPC-server) 구조 파악 + SET_ROUTE/START_PATROL 경합조건 근본원인

- 구성: `DAE-vlm-main`(FastAPI, Qwen-VL 기반), `DAE_Backend-main`(Spring Boot), `DAE_Frontend`(React)
- ⚠ `/home` 디스크 92% - `DAE_Backend-main/videodata`(mp4 350개+), `wavdata`(wav 100개+)가 주 원인 추정. 팀 확인 없이 삭제 안 함 - 공유 필요.
- **경합조건 근본원인**: `WebSocketConfig.java`가 Spring 기본 SimpleBroker를 `taskExecutor` 지정 없이 사용 → STOMP 발송이 멀티스레드 풀 처리되어 같은 채널 메시지도 도착순서 미보장. 프론트(`StartPatrolModal.jsx`)는 `await saveRoute()` 후 `await startPatrol()`로 정확히 순차 호출하므로 결백 - 다만 SET_ROUTE 컨트롤러가 STOMP 전송 전 외부 고도조회 API를 거쳐 원래 더 느림.
- 제안 수정안(서버팀 협의 필요, 미적용): ① `clientOutboundChannel` 단일스레드화 ② `SET_ROUTE`+`START_PATROL` 통합 명령 신설 ③ 드론쪽에서 route 없으면 짧게 대기 후 폴백

## 6. 스트리밍 끊김/캡처 프레임드롭 진단 + 완화

- 제보: 드론 자체추론+서버스트리밍 동시 진행시 캡처 프레임드롭, 스트리밍 끊김
- 원인 추론: `main.py` 캡처루프가 동기/단일스레드라 버퍼 9프레임 이후 "매 프레임"마다 YOLO+WideBranchNet을 캡처루프 안에서 동기 실행. 목표 프레임간격(111ms) 초과 시 GStreamer `appsink(drop=true)`가 프레임을 버려 캡처드롭 + `latest_frame_for_stream` 갱신도 뜸해져 스트리밍도 끊김. GIL 경합도 가중.
- 진단용 계측 코드 추가(`main.py` 캡처루프 밀린시간, `anomaly_model.py` 단계별 소요시간) - 커밋됨. 실제 검증은 사람이 카메라에 잡혀 WBN추론이 도는 상황이 필요했으나 세션 중 미검증(YOLO 미검출로 조기반환만 계속됨)
- 완화 조치(검증 전 우선 적용): 추론 주기를 매프레임 → 2초(18프레임)에 한 번으로 스로틀링
- `DEVICE_KEY` 기본값 `HPC-2026`을 `start_all.sh`에 지정 → 스트리밍 인증오류가 401(키문제)→410(정상, 시청자없음)로 정상화 확인
- ⚠ 아직 실사람+실시청 상태에서 `[진단]` 로그로 최종검증 못함 - 다음 실기 테스트 때 확인 필요

## 7. server↔jetson 명령/텔레메트리 정합성 분석 + git 백업 전체를 `Capstone_drone`으로 통합

**정합성 분석 결과:**
- 명령 액션 10종(`START_PATROL` 등) 서버↔드론 문자열 일치 확인 - 문제없음
- `status`/`currentAction` 값(`IDLE/PATROLLING/PAUSED/RETURNING/LANDING`, `PATROL_COMPLETE`/`RETURN_COMPLETE`/`BATTERY_RTL`) 전부 일치 - 문제없음
- `ts`, `msg_type` 필드: 서버 DTO엔 있지만 백엔드·프론트 어디서도 안 읽음(죽은 필드, 실질 영향 없음)
- **`hasRoute` 필드 누락 발견·수정**: 서버 `Telemetry.java`/프론트가 실제로 사용하는 필드인데 드론이 아예 안 보내고 있었음(프론트는 `?? true`로 안전 폴백해 크래시는 없었지만, "경로 없을 때 재개 막기" 안전장치가 사실상 무력화). `command_receiver.py`의 `self._route`(SET_ROUTE로 채워지고 CANCEL_PATROL/EMERGENCY_STOP으로 비워짐)를 `_write_status_state()`에 `hasRoute`로 실어 `telemetry_sender.py`가 전달하도록 수정. 서버의 `GET /{droneId}/route` 주석("실제 보유 여부는 텔레메트리의 hasRoute가 진실")과 정확히 부합하는 방향으로 판단해 진행.
- `station` 필드도 드론이 안 보내고 있음(프론트는 폴백 있어 덜 심각, 별도 개선 여지)

**백업정책 최종 통합**: 사용자 요청으로 코드(git)뿐 아니라 문서(Drive)까지 전부 로컬+GitHub 단일 저장소로 통합.

- 신규 저장소: `https://github.com/miru0522/Capstone_drone` (private). 기존 `drone_2026` 저장소는 폐기(내용은 `jetson-live` 브랜치로 이전 후 삭제 예정 - `delete_repo` 스코프 재인증 필요해 사용자 액션 대기 중)
- 구조:
  ```
  docs/md/    - CLAUDE.md
  docs/docs/  - 참고문서(이 파일 포함, Drive 요약본 등)
  jetson/code/   - Jetson 코드 스냅샷 (main 브랜치)
  jetson/models/ - 양자화 전 원본 ONNX (widebranchnet_n9.onnx, yolov5n.onnx - Jetson의 /home/yunseon/{integration_bench,detector_bench}/onnx/에서 확보)
  server/     - DAE-vlm-main/DAE_Backend-main/DAE_Frontend 소스코드만(weights 9.2G/videodata 1.1G/wavdata 44M/profiledata/secret.yaml 등 제외 - 서버팀이 이미 준비해둔 /home/yunseon/Capstone/.gitignore 기준을 그대로 이식, 우리가 고른 제외항목과 정확히 일치했음)
  ```
- **`jetson-live` 브랜치**: Jetson 실기의 `~/drone_2026/code/`가 구조 변경 없이 그대로 직접 push하는 브랜치(전체 히스토리 보존). 사용자 판단: Jetson 실제 폴더명·경로는 "2026 연구자료" 성격상 `drone_2026`을 계속 유지 - git 안의 "jetson/" 이름은 저장소 정리용일 뿐 실기 경로 변경 아님.
- 서버 코드(`server/`)는 이번 세션엔 1회성 스냅샷 반영. 지속 동기화 방식은 미정.
- 문서 백업도 이 세션부터 `docs/docs/`로 전환(Drive는 과거 이력 조회용으로만 유지)

## 다음에 할 일

1. [사용자 명시 요청, 진행중] 서버 명령↔드론 실행/텔레메트리 보고 일치 검증 - 명령/상태값/hasRoute까지 확인 완료. 남은 것: `hasRoute`가 실제 SET_ROUTE 흐름에서 `true`로 바뀌는지 실기(또는 서버팀 협조) 검증
2. 스트리밍 끊김/프레임드롭 완화(2초 추론 스로틀링) 효과 검증 - 사람 있는 상태 + 실제 시청 상태에서 `logs/main.log`의 `[진단]` 로그로 확인
3. PX4 착륙 파라미터(`MPC_LAND_SPEED` 등) 실기 조회해서 감속 동작 실제로 있는지 확인 (드론 Python 코드엔 감속로직 없음 확인됨 - `action.land()` 한 줄, PX4 펌웨어 파라미터에 전적으로 의존)
4. `CANCEL_PATROL` 상태 무관 수락 건 - 실기 테스트로 LANDING 중 `hold()` 동작 검증
5. `SET_ROUTE`/`START_PATROL` 경합조건 수정안(3가지 중 택1) - 서버팀과 협의 후 진행
6. 서버 `/home` 디스크 92% - `videodata`/`wavdata` 정리 필요 여부 팀과 상의
7. `drone_2026` GitHub 저장소 삭제 마무리 (`gh auth refresh -h github.com -s delete_repo` 후)
8. `server/` 소스코드를 어떤 주기/방식으로 계속 동기화할지 결정 (지금은 1회성 스냅샷)
