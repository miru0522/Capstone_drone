# 서버 분석 로깅 소스 반영 및 검수 기록 — 2026-09-25

## 결과와 적용 범위

비디오 수신부터 VideoMAE 점수, Qwen 분석, TTS, 백엔드 저장 및 알림까지 연결하는 JSONL 로깅을 추가했다. Claude Code가 실제 변경 diff·소스·테스트 증거를 읽고 코드 검수 1회차에서 **COMPLETE_APPROVED**를 반환했다. 승인 후 동일 해시의 소스 7개를 HPC 서버 `/home/yunseon/Capstone/`에 반영했다.

**소스 반영 완료와 운영 활성화는 다르다.** 현재 실행 중인 `dae_ai_server`·`dae_backend` 컨테이너는 재시작·재생성하지 않았다. AI 컨테이너는 소스 bind mount가 없으므로 호스트 파일 변경이 현재 프로세스에 자동 반영되지 않는다. 로그 회전 override도 아직 활성화되지 않았다. AGENTS.md의 `dae_*` 프로세스 종료 금지 규칙을 준수했다. 실행 대상은 HPC 서버이며 PC에서는 개발·격리 테스트·검수만 수행했다. Jetson은 변경하지 않았다.

## 변경 전 백업

- 기존 GitHub main: `056a5fbef778915ec1a023321cc2a4219eb0e9a4`.
- 실제 운영 AI 소스가 GitHub보다 최신이어서 한국어 프롬프트·후처리·TTS 동작을 보존한 기준본을 먼저 만들었다.
- 기준 커밋: `c58bea76d287c340b219b0295d89eb17fa9a17de`.
- GitHub `backup/server-logging-baseline-20260924` 브랜치의 동일 커밋을 원격 조회로 확인한 후 변경을 반영했다.
- 서버의 기존 app.py, VideoMAE wrapper, EventController.java 옆에 `.bak_20260925_approved_logging` 원본 백업을 남겼다. 원본 해시와 백업 해시 일치 확인 완료.
- 서버 전체 스냅샷 백업은 아니다. 가중치·미디어·인증정보는 포함하지 않았다.

## 기록하는 항목

| 구간 | 로깅 항목 |
|---|---|
| 수신·검증 | analysis_id, 요청 본문 수신량, 검증 결과, 임시 저장 결과 |
| VideoMAE | 디코딩/전처리, binary/subclass 단계, 점수와 판정, 소요시간 |
| Qwen | 준비, lock 대기, 생성, 파싱, 필수 출력 유무, 소요시간 |
| 후처리 | TTS 결과, 메타데이터 변경 필드와 허용된 값, 제출 데이터 요약, 영상 변환·fallback |
| AI→백엔드 | X-Analysis-Id, 제출 결과와 HTTP 상태, 응답 유실 시 unknown |
| 백엔드 | JSON 파싱, 영상·음성 저장, 필드 매핑, DB 저장, event_id, STOMP 발행 |
| 종료·실패 | 요청별 최종 상태, 단계별 상태, 정리 결과, 오류 종류와 스택 위치, 부분 성공 |

영상·오디오 원문, 생성 전문, 프롬프트, 인증키, 예외 메시지 및 소스 텍스트는 신규 로그에 넣지 않는다. 기존 분석·점수·응답 정책은 유지한다. stdout 동기 출력이므로 장시간 출력 블로킹 성능까지 보장하지는 않는다. 모델 식별자는 이름·크기·mtime·프롬프트 해시 기반이며 가중치 내용 해시가 아니다.

## 검수와 검증

- [Claude 최종 승인 원문](server_logging_review_20260925/claude-code-review-round1-approved.md)
- [검수 요청 및 범위](server_logging_review_20260925/review-request.md)
- [로깅 변경만 포함한 diff](server_logging_review_20260925/changes.diff)
- [승인본 SHA256 목록](server_logging_review_20260925/manifest.json)
- Python 26건 통과: 요청 격리, 실패 상태, 민감 문자열 비노출, 기존 원본 대비 4가지 응답 동일성, VideoMAE 판정 경계 등.
- Java 5건 통과: ID 연결, 파일/DB/STOMP 부분 실패, 필드 폴백, 잘못된 JSON 등. JDK21로 컴파일·실행.
- 실제 GPU·DB·STOMP는 mock으로 대체한 테스트이며 운영 E2E를 완료했다고 주장하지 않는다.
- 승인 후 9개 파일의 해시를 다시 검증했다. 서버 반영 대상 7개는 전체 파일을 다시 읽어 해시 일치와 원본 백업을 검증했다. 서버 컨테이너 Python으로 반영된 Python 3개 파일의 AST 문법 검사 통과(모델 import 없음).
- [서버 반영 후 검증 결과](server_logging_review_20260925/server-source-verification.txt)
- 서버 확인 시 AI 컨테이너 시작 시각은 `2026-09-19T12:05:38Z`, 백엔드는 `2026-09-19T09:33:50Z`로 기존 프로세스 유지 확인.

## 다음에 할 일

1. 서버 담당자와 운영 활성화 시점을 정하고 승인된 소스로 AI/백엔드 이미지를 빌드·배포한다. `runtime_sources/videomae/videomae_infer.py`는 서버의 `DAE-vlm-main/weights/videomae/videomae_infer.py`에 대응한다.
2. 로그 회전은 기존 compose와 `compose.analysis-logging.yml`을 함께 사용하는 배포에서 적용한다. 서비스별 20MB×5개 설정이며 실제 회전 검증은 남아 있다.
3. 정상/이상 영상의 실제 GPU 분석→DB 저장→관제 알림 흐름을 점검한다. 동일 analysis_id와 event_id 연결, 점수, 단계 상태, 로그량을 확인한다.
4. 실제 부하·강제 종료·OOM 상황 검증은 별도로 수행한다. 현재 테스트로 이 범위까지 검증되었다고 간주하지 않는다.
